@AGENTS.md

## Claude-специфика

- Перед изменениями в `apps/api/src/modules/conversion` и `apps/api/src/modules/auth` — сначала план, потом код.
- Новая сессия на каждую задачу из `tasks.md`. Длинная сессия после сжатия контекста теряет вложенные правила.
- Перед кодом по задаче из `tasks.md` — новая ветка от `main`, имя `NNN-slug` по номеру и слагу из реестра `SPECS.md` (например `004-error-contract`). Коммиты этой задачи — в неё, не в `main` напрямую: `SPECS.md` требует «одна фича — одна ветка — один PR».
- Правила с областью действия живут в `.claude/rules/*.md` с фронтматтером `paths`. В этот файл они не переносятся.

<!--
Проверка, что всё загрузилось:
  /memory   — должны быть перечислены CLAUDE.md и AGENTS.md
  /context  — сколько контекста занято и чем
Если AGENTS.md в списке нет, импорт не сработал.
-->
