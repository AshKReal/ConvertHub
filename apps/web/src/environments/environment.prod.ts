/**
 * Спека 017. Пустой `apiUrl` здесь — не заглушка, а рабочее значение.
 *
 * Фронт и API в проде живут на одном origin: статика на Vercel, а `/v1/*`
 * оттуда же переписывается на Railway (`vercel.json`). Обратный прокси выбран
 * ради refresh-cookie: на разных сайтах (`*.vercel.app` против
 * `*.up.railway.app`) `SameSite=Lax` не отправляется, и сессия умирала бы через
 * 15 минут — а `SameSite=None` был бы отступлением от `AUTH-RULES.md` §35 и всё
 * равно не работал бы в Safari. Обоснование целиком — `docs/DEPLOYMENT.md` §9.
 *
 * Поэтому запросы уходят относительными путями `/v1/...`, и домен API сюда
 * вписывать не нужно. Кто отличает «наш» запрос от чужого — `core/api-url.ts`;
 * читать его до правки этого файла обязательно.
 */
export const environment = {
  production: true,
  apiUrl: '',
};
