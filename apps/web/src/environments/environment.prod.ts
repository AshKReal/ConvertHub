/**
 * Заполняется спекой 017 (деплой, Railway + Vercel) вместе с реальным
 * доменом API — сейчас его не существует, подставлять что-то правдоподобное
 * означало бы гадать.
 */
export const environment = {
  production: true,
  apiUrl: '', // TODO(017): реальный домен API при деплое
};
