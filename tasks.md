# tasks.md

Гранулярный чек-лист поверх реестра `SPECS.md`. Один пункт верхнего уровня = одна строка реестра = один цикл спека→план→чек-лист (`SPECS.md`, раздел «Цикл»). Подпункты — не замена цикла, а разбивка «что вообще входит», на которую опирается раздел «Входит» будущей спеки.

**Правило CLAUDE.md:** новая сессия на каждую задачу из этого файла. Длинная сессия после сжатия контекста теряет вложенные правила из `.claude/rules/*.md`.

**Порядок — фронтенд целиком (стадии 0–3), потом бэкенд и Docker (стадии 4–11).** Экраны фронтенд-фазы строятся на локальных моках, без сети. Подключение к реальному API — отдельные подпункты внутри бэкендовых спек, помечены «→ подключение». Обоснование порядка — `TECH-SPEC.md`, раздел 14; сводная таблица стадий — там же.

Для стадий 0–3 Docker не нужен. Перед стартом любого пункта стадии 4 и далее — `docker compose up -d` (postgres, redis подняты с этого момента, `ARCHITECTURE.md` §1.1). 🔒 — план сначала, код после, построчная проверка владельцем (`.claude/rules/critical-zones.md`).

---

# Фронтенд-фаза

## Стадия 0 — каркас: тема, layout, языки

- [ ] **024** `home-page-ui`
  - [x] Главная: заголовок, четыре карточки направлений (`JPG→PNG`, `PNG→JPG`, `DOCX→PDF`, `PDF→JPG`)
  - [x] `shared/ui/format-card` — без доменных типов, принимает `from`/`to`/`description`/`icon`
  - [x] Иконки различаются по категории формата (изображение / документ / документ со строками), не одна на все карточки
  - [x] Реестр направлений в `packages/shared` — общий для главной, страницы конвертации и будущего бэкенда
  - [x] Карточка — ссылка на `/convert/:direction`; маршрут и каркас страницы заведены здесь, наполнение зоны загрузки — 001/006
  - [x] Неизвестное направление в адресе — возврат на главную (`canActivate`), без отдельного экрана 404
  - [x] Задним числом написана `specs/024-home-page-ui.md`
  - [ ] Приёмка владельцем: критерии из спеки руками, обе темы, враждебное второе мнение
- [ ] **025** `app-shell-layout`
  - [x] `core/services/theme.ts` — сигнал темы, `toggle()`/`setTheme()`, класс `.dark` на `<html>`, `localStorage`
  - [x] Инлайн-скрипт в `index.html`, проставляет тему до первой отрисовки (иначе мелькает светлый фон)
  - [x] `core/layout/layout.ts` — шапка с местом под будущую навигацию и переключателем темы
  - [x] Токены `DESIGN.md` (палитра, шрифты, радиусы, тени) в `apps/web/src/styles.css`
  - [x] Название продукта в шапке — ссылка на главную
  - [ ] Пункты навигации в шапке — не сюда, добавляются вместе с экранами, которым есть куда вести (019–023)
  - [x] Задним числом написана `specs/025-app-shell-layout.md`
  - [ ] Приёмка владельцем: критерии из спеки руками, обе темы, враждебное второе мнение
- [ ] **027** `i18n-foundation` (вынесено из 004: механика понадобилась до первых экранов)
  - [x] Локали `ru`/`uk`/`en`, английская — умолчание
  - [x] Определение по стране без сети: часовой пояс (Россия → `ru`, Украина → `uk`), затем регион языкового тега
  - [x] Ручной переключатель в шапке, выбор в `localStorage` и приоритетнее автоопределения
  - [x] Английский словарь — источник ключей, `ru`/`uk` типизированы через него: забытый перевод не компилируется
  - [x] Плейсхолдеры `{name}`, `<html lang>` и заголовок вкладки следуют за локалью
  - [x] Главная, страница конвертации и шапка переведены на все три языка
  - [ ] Приёмка владельцем: три часовых пояса, переключение на обоих экранах, обе темы

## Стадия 1 — зона загрузки и контракт ошибок

- [ ] **001** `upload-dropzone` — `specs/001-upload-dropzone.md`
  - [x] Форма `POST /v1/convert` на клиенте (`FormData` без ручного `Content-Type`) — контракт задаётся здесь, бэкенд (002) подстраивается под него, не наоборот
  - [x] Zod-схема полей запроса и лимит размера — в `packages/shared`, реестр направлений дополнен MIME и `target`
  - [x] Состояния `empty → dragover → selected`, на моках (без реального запроса)
  - [x] Счётчик глубины `dragenter`/`dragleave` против мигания
  - [x] На мобильных — кнопка вместо `dragover`
  - [ ] Кнопка запуска в состоянии `selected` — перенесена в 006: она включает автомат, которого ещё нет
  - [ ] Приёмка владельцем: тест-кейсы из спеки, обе темы, три языка, 320px
- [ ] **004** `error-contract` — `specs/004-error-contract.md`
  - [x] `packages/shared/src/constants/error-codes.ts` — `as const`, поля `status`/`retryable`
  - [x] Тип `AppError`, `errorInterceptor` на клиенте маппит в него (пока не на что мапить — заготовка под 026)
  - [x] Тексты по коду ошибки (ru/uk/en) с конкретными числами, не «Ошибка загрузки» — в словарь из 027, механика локалей уже есть
  - [ ] Бэкендовая часть (`AllExceptionsFilter`, `problem+json`) — не сюда, это 026 в бэкенд-фазе
  - [ ] Приёмка владельцем: критерии из спеки руками, враждебное второе мнение
- [ ] **006** `dropzone-full-states` — `specs/006-dropzone-full-states.md`
  - [x] Полный автомат: `uploading`, `converting`, `done`, `error`, `quotaFull` — прогресс на `setTimeout`, не на реальном запросе
  - [x] Отмена загрузки (пока просто возврат в `empty`, реальный обрыв HTTP — при подключении в 005)
  - [x] Покрытие кодов ошибок из 004 по местам показа: зона (файл отклонён) и тост (сбой конвертера) — баннер и инлайн нечем наполнить раньше 026/019, см. спеку
  - [x] `quotaFull` — на захардкоженном значении квоты, реальный расчёт — в 010
  - [ ] Приёмка владельцем: критерии из спеки руками, обе темы, три языка, 320px

## Стадия 2 — экраны аутентификации

- [ ] **019** `auth-screens-ui` — `specs/019-auth-screens-ui.md`
  - [x] Экран входа: email + пароль, единое сообщение об ошибке на моке
  - [x] Экран регистрации
  - [x] Кнопки «Войти через Google» и Telegram Login — визуально, без реального OAuth/HMAC
  - [x] Состояние «вошёл» в шапке `Layout` (имя/аватар-заглушка, кнопка выхода) — переключается локальным моком, не сессией
  - [x] Валидация форм на клиенте (формат email, длина пароля) — серверная часть в 007
  - [ ] Приёмка владельцем: критерии из спеки руками, обе темы, три языка, 320px
- [ ] **020** `account-pages-ui`
  - [ ] Экран «Забыли пароль» — форма, экран «проверьте почту»
  - [ ] Экран сброса пароля по токену (сам токен — заглушка в URL)
  - [ ] Профиль: смена пароля (форма открывается, сабмит — заглушка), список привязанных провайдеров (мок), удаление аккаунта (модалка подтверждения, без реального запроса)

## Стадия 3 — экраны файлов, ключей, документации

- [ ] **021** `files-page-ui`
  - [ ] Список файлов на моковом массиве, курсорная пагинация — кнопка «Показать ещё» на фейковом курсоре
  - [ ] Переключатель `save` на каждой строке (локальное состояние, без запроса)
  - [ ] Индикатор занятого места — на захардкоженном значении квоты
  - [ ] Пустое состояние (файлов нет) и состояние загрузки списка (скелетоны)
- [ ] **022** `api-keys-page-ui`
  - [ ] Список ключей (маскированные, `ch_live_a1b2••••••••`) на моке
  - [ ] Модалка выпуска нового ключа — полный ключ показывается один раз, копирование в буфер
  - [ ] Перевыпуск и отзыв — подтверждающая модалка, состояние меняется локально
- [ ] **023** `api-docs-page-ui`
  - [ ] Страница `/api-docs`: разделы по эндпоинтам на заглушечной схеме (реальная OpenAPI — в 013)
  - [ ] Примеры интеграции (`curl -o`, заголовок `Bearer`) как статичные блоки кода, моноширинный токен `DESIGN.md`

---

# Бэкенд-фаза

Перед стартом — `docker compose up -d` (postgres, redis).

## Стадия 4 — проверка движков

- [ ] **000** `engine-quality-check`
  - [ ] sharp: JPG↔PNG на 20–30 реальных файлах, зафиксировать качество и время
  - [ ] `soffice --headless --convert-to pdf` на 20–30 реальных docx, зафиксировать проблемные случаи (пароль, кастомные шрифты, макросы)
  - [ ] PDF→JPG (pdfjs либо poppler) на 20–30 файлах, DPI 72/150/300, лимит 50 страниц
  - [ ] Решение по PDF-движку (pdfjs vs poppler) зафиксировать в TECH-SPEC.md

## Стадия 5 — `JPG → PNG`, хранилище, глобальный фильтр ошибок

- [ ] **002** 🔒 `convert-jpg-png`
  - [ ] Приём файла потоком во временный каталог — под контракт, заданный в 001
  - [ ] Определение типа по magic bytes, не по расширению/`Content-Type`
  - [ ] Decompression bomb: проверка заявленных размеров до декодирования, лимит 50 Мп
  - [ ] `ConversionEngine.convert()` для sharp: `JPG↔PNG`, `quality`, `background`
  - [ ] Удаление временных файлов в `finally`
- [ ] **003** 🔒 `file-storage`
  - [ ] Интерфейс `Storage` (`put`/`getSignedUrl`/`delete`/`list`)
  - [ ] `LocalDiskStorage implements Storage`, папка вне репозитория
  - [ ] Таблицы `files` и `conversions`, ULID
  - [ ] Транзакция: insert `files` + update `users.storage_used_bytes`
  - [ ] `GET /v1/files/{id}/download` → `302` на подписанную ссылку (TTL 15 мин)
- [ ] **026** `error-contract-backend`
  - [ ] `AllExceptionsFilter` → `problem+json` + `request_id`, коды из `packages/shared` (004)
  - [ ] Подключить `errorInterceptor` (004) к реальным ответам API
  - [ ] Приёмка: зона загрузки (001/006) показывает настоящие коды ошибок, не моковые

## Стадия 6 — остальные направления, подключение зоны загрузки

- [ ] **005** `conversion-engines` (`PNG→JPG`, `PDF→JPG`)
  - [ ] `PNG→JPG`: `quality` 60–100, `background`
  - [ ] `PDF→JPG`: движок по итогам 000, `dpi` 72/150/300, лимит 50 страниц
  - [ ] Лимит одновременных конвертаций на пользователя (3)
  - [ ] `ConversionEngine` проектируется под HTTP-движок заранее (018 не должен потребовать менять интерфейс)
  - [ ] Подключить 001/006 к реальному `POST /v1/convert`: прогресс через `HttpEventType`, переход в «конвертируем» по 100% загрузки, не по `Response`; реальная отмена — обрыв HTTP

## Стадия 7 — аутентификация

- [ ] **007** 🔒 `auth-native`
  - [ ] Регистрация email + пароль
  - [ ] Вход, единое сообщение «Неверный email или пароль» на любую причину отказа
  - [ ] JWT access (15 мин, в памяти) + refresh (30 дней, `HttpOnly`/`Secure`/`SameSite=Lax`)
  - [ ] Ротация refresh при обновлении; повторное предъявление использованного токена → завершение всех сессий
  - [ ] Клиентская очередь на параллельные `401` (не тройной вызов `/auth/refresh`)
  - [ ] Выход
  - [ ] Подключить 019 (вход/регистрация/выход) к реальным эндпоинтам
- [ ] **008** 🔒 `auth-providers`
  - [ ] Google OAuth 2.0 Authorization Code + PKCE, идентификация по `sub`, не по email
  - [ ] Telegram Login Widget: серверная проверка HMAC-SHA256 подписи, `auth_date` не старше 24 ч
  - [ ] Таблица `identities` (`provider` + `provider_uid`)
  - [ ] Привязка внешнего аккаунта только при подтверждённом email от провайдера
  - [ ] Запрет отвязки последнего способа входа
  - [ ] Подключить кнопки Google/Telegram из 019 к реальному потоку
- [ ] **009** 🔒 `account-recovery`
  - [ ] Восстановление пароля: одинаковый ответ независимо от существования аккаунта
  - [ ] Токен ≥32 случайных байт, в БД хеш, TTL 30 мин, одноразовый; Telegram-код — TTL 10 мин, ≤5 попыток
  - [ ] Смена пароля из профиля → завершение всех сессий + уведомление
  - [ ] Удаление аккаунта
  - [ ] Подключить экраны из 020 к реальным эндпоинтам

## Стадия 8 — хранилище, квота, публичный API

- [ ] **010** `user-files-quota`
  - [ ] Курсорная пагинация в БД (`WHERE id < :cursor ORDER BY id DESC`, ULID)
  - [ ] Переключатель `save`, автоснятие при заполненной квоте с предупреждением
  - [ ] Реальный расчёт `storage_used_bytes`
  - [ ] TanStack Query: список файлов + инвалидация `['me']` после мутаций — подключение 021 к реальным данным
- [ ] **011** 🔒 `api-keys`
  - [ ] Выпуск: `ch_` + `live`/`test` + 32 случайных символа
  - [ ] В БД — только SHA-256 хеш + префикс для отображения (`ch_live_a1b2`)
  - [ ] Полный ключ показывается один раз, при выпуске и перевыпуске
  - [ ] Перевыпуск инвалидирует предыдущий ключ; `last_used_at`
  - [ ] Подключить 022 к реальным эндпоинтам выпуска/перевыпуска/отзыва
- [ ] **012** 🔒 `public-api`
  - [ ] Bearer-аутентификация по API-ключу
  - [ ] Rate limit token bucket в Redis: гость 5/час (по хешу IP), пользователь 100/сутки, 60 запросов/мин к API
  - [ ] `Idempotency-Key` (UUID, 24 часа) через Redis
  - [ ] Redis недоступен → fail-open для rate limit, fail-closed для проверки сессии/ключа

## Стадия 9 — документация, наблюдаемость, тесты

- [ ] **013** `api-docs`
  - [ ] OpenAPI-схема, сгенерированная из Zod-схем `packages/shared`
  - [ ] Подключить 023 к реальной схеме вместо заглушки
- [ ] **014** `observability`
  - [ ] `GET /health` и `GET /ready`
  - [ ] `request_id` генерируется на клиенте, интерцептор пробрасывает в заголовок и в лог
  - [ ] Метрики: конвертации по направлению/статусу, гистограмма длительности, доля ошибок по коду, заполненность пула, объём хранилища
- [ ] **015** `testing`
  - [ ] Vitest: валидаторы, автомат зоны загрузки, pipes, расчёт квоты
  - [ ] Testcontainers: транзакции и миграции на настоящем PostgreSQL
  - [ ] Playwright: гость конвертирует; пользователь входит и видит файл; файл >10 МБ отклоняется; конвертация при заполненной квоте не сохраняет файл

## Стадия 10 — полный docker compose, `DOCX → PDF`

- [ ] **016** `containerization`
  - [ ] Расширить `docker-compose.yml`: `gotenberg` (без сети наружу, `read_only`, `tmpfs /tmp`, `cap_drop: ALL`, лимит памяти), `minio`
  - [ ] `S3Storage implements Storage`, привязка в DI вместо `LocalDiskStorage` (проверочное свойство: ноль строк в `conversion.service.ts`)
  - [ ] `docker/api.Dockerfile`, multi-stage
  - [ ] `.github/workflows/ci.yml`
- [ ] **018** 🔒 `docx-pdf`
  - [ ] `DocumentEngine implements ConversionEngine` — HTTP POST в Gotenberg
  - [ ] Таймаут `CONVERSION_TIMEOUT`, семафор пула документов (8), ожидание до 10 с → `503` + `Retry-After`
  - [ ] Подключение второй реализации `ConversionEngine` без изменения сигнатуры интерфейса (005) и без изменения `conversion.service.ts`

## Стадия 11 — развёртывание

- [ ] **017** `deployment`
  - [ ] Railway: `api` + `gotenberg` (без публичного домена) + managed Postgres/Redis
  - [ ] Vercel: Angular, Output Directory `dist/web/browser`
  - [ ] `CORS_ORIGIN` — конкретный домен, не `*`
  - [ ] `prisma migrate deploy` при старте контейнера
  - [ ] README для внешнего читателя
