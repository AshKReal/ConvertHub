# tasks.md

Гранулярный чек-лист поверх реестра `SPECS.md`. Один пункт верхнего уровня = одна строка реестра = один цикл спека→план→чек-лист (`SPECS.md`, раздел «Цикл»). Подпункты — не замена цикла, а разбивка «что вообще входит», на которую опирается раздел «Входит» будущей спеки.

**Правило CLAUDE.md:** новая сессия на каждую задачу из этого файла. Длинная сессия после сжатия контекста теряет вложенные правила из `.claude/rules/*.md`.

**Порядок — фронтенд целиком (стадии 0–3), потом бэкенд и Docker (стадии 4–11).** Экраны фронтенд-фазы строятся на локальных моках, без сети. Подключение к реальному API — отдельные подпункты внутри бэкендовых спек, помечены «→ подключение». Обоснование порядка — `TECH-SPEC.md`, раздел 14; сводная таблица стадий — там же.

Для стадий 0–3 Docker не нужен. Перед стартом любого пункта стадии 4 и далее — `docker compose up -d` (postgres, redis подняты с этого момента, `ARCHITECTURE.md` §1.1). 🔒 — план сначала, код после, построчная проверка владельцем (`.claude/rules/critical-zones.md`).

---

# Фронтенд-фаза

## Стадия 0 — каркас: тема, layout, языки

- [x] **024** `home-page-ui`
  - [x] Главная: заголовок, четыре карточки направлений (`JPG→PNG`, `PNG→JPG`, `DOCX→PDF`, `PDF→DOCX`)
  - [x] `shared/ui/format-card` — без доменных типов, принимает `from`/`to`/`description`/`icon`
  - [x] Иконки различаются по категории формата (изображение / документ / документ со строками), не одна на все карточки
  - [x] Реестр направлений в `packages/shared` — общий для главной, страницы конвертации и будущего бэкенда
  - [x] Карточка — ссылка на `/convert/:direction`; маршрут и каркас страницы заведены здесь, наполнение зоны загрузки — 001/006
  - [x] Неизвестное направление в адресе — возврат на главную (`canActivate`), без отдельного экрана 404
  - [x] Задним числом написана `specs/024-home-page-ui.md`
  - [x] Приёмка владельцем: критерии из спеки руками, обе темы, враждебное второе мнение
- [x] **025** `app-shell-layout`
  - [x] `core/services/theme.ts` — сигнал темы, `toggle()`/`setTheme()`, класс `.dark` на `<html>`, `localStorage`
  - [x] Инлайн-скрипт в `index.html`, проставляет тему до первой отрисовки (иначе мелькает светлый фон)
  - [x] `core/layout/layout.ts` — шапка с местом под будущую навигацию и переключателем темы
  - [x] Токены `DESIGN.md` (палитра, шрифты, радиусы, тени) в `apps/web/src/styles.css`
  - [x] Название продукта в шапке — ссылка на главную
  - [ ] Пункты навигации в шапке — не сюда, добавляются вместе с экранами, которым есть куда вести (019–023)
  - [x] Задним числом написана `specs/025-app-shell-layout.md`
  - [x] Приёмка владельцем: критерии из спеки руками, обе темы, враждебное второе мнение
- [x] **027** `i18n-foundation` (вынесено из 004: механика понадобилась до первых экранов)
  - [x] Локали `ru`/`uk`/`en`, английская — умолчание
  - [x] Определение по стране без сети: часовой пояс (Россия → `ru`, Украина → `uk`), затем регион языкового тега
  - [x] Ручной переключатель в шапке, выбор в `localStorage` и приоритетнее автоопределения
  - [x] Английский словарь — источник ключей, `ru`/`uk` типизированы через него: забытый перевод не компилируется
  - [x] Плейсхолдеры `{name}`, `<html lang>` и заголовок вкладки следуют за локалью
  - [x] Главная, страница конвертации и шапка переведены на все три языка
  - [x] Приёмка владельцем: три часовых пояса, переключение на обоих экранах, обе темы

## Стадия 1 — зона загрузки и контракт ошибок

- [x] **001** `upload-dropzone` — `specs/001-upload-dropzone.md`
  - [x] Форма `POST /v1/convert` на клиенте (`FormData` без ручного `Content-Type`) — контракт задаётся здесь, бэкенд (002) подстраивается под него, не наоборот
  - [x] Zod-схема полей запроса и лимит размера — в `packages/shared`, реестр направлений дополнен MIME и `target`
  - [x] Состояния `empty → dragover → selected`, на моках (без реального запроса)
  - [x] Счётчик глубины `dragenter`/`dragleave` против мигания
  - [x] На мобильных — кнопка вместо `dragover`
  - [ ] Кнопка запуска в состоянии `selected` — перенесена в 006: она включает автомат, которого ещё нет
  - [x] Приёмка владельцем: тест-кейсы из спеки, обе темы, три языка, 320px
- [x] **004** `error-contract` — `specs/004-error-contract.md`
  - [x] `packages/shared/src/constants/error-codes.ts` — `as const`, поля `status`/`retryable`
  - [x] Тип `AppError`, `errorInterceptor` на клиенте маппит в него (пока не на что мапить — заготовка под 026)
  - [x] Тексты по коду ошибки (ru/uk/en) с конкретными числами, не «Ошибка загрузки» — в словарь из 027, механика локалей уже есть
  - [ ] Бэкендовая часть (`AllExceptionsFilter`, `problem+json`) — не сюда, это 026 в бэкенд-фазе
  - [x] Приёмка владельцем: критерии из спеки руками, враждебное второе мнение
- [x] **006** `dropzone-full-states` — `specs/006-dropzone-full-states.md`
  - [x] Полный автомат: `uploading`, `converting`, `done`, `error`, `quotaFull` — прогресс на `setTimeout`, не на реальном запросе
  - [x] Отмена загрузки (пока просто возврат в `empty`, реальный обрыв HTTP — при подключении в 005)
  - [x] Покрытие кодов ошибок из 004 по местам показа: зона (файл отклонён) и тост (сбой конвертера) — баннер и инлайн нечем наполнить раньше 026/019, см. спеку
  - [x] `quotaFull` — на захардкоженном значении квоты, реальный расчёт — в 010
  - [x] Приёмка владельцем: критерии из спеки руками, обе темы, три языка, 320px

## Стадия 2 — экраны аутентификации

- [x] **019** `auth-screens-ui` — `specs/019-auth-screens-ui.md`
  - [x] Экран входа: email + пароль, единое сообщение об ошибке на моке
  - [x] Экран регистрации
  - [x] Кнопки «Войти через Google» и Telegram Login — визуально, без реального OAuth/HMAC
  - [x] Состояние «вошёл» в шапке `Layout` (имя/аватар-заглушка, кнопка выхода) — переключается локальным моком, не сессией
  - [x] Валидация форм на клиенте (формат email, длина пароля) — серверная часть в 007
  - [x] Приёмка владельцем: критерии из спеки руками, обе темы, три языка, 320px
- [x] **020** `account-pages-ui`
  - [x] Экран «Забыли пароль» — форма, экран «проверьте почту»
  - [x] Экран сброса пароля по токену (сам токен — заглушка в URL)
  - [x] Профиль: смена пароля (форма открывается, сабмит — заглушка), список привязанных провайдеров (мок), удаление аккаунта (модалка подтверждения, без реального запроса)
  - [x] Приёмка владельцем: критерии из спеки руками, обе темы, три языка, 320px

## Стадия 3 — экраны файлов, ключей, документации

- [x] **021** `files-page-ui`
  - [x] Список файлов на моковом массиве, курсорная пагинация — кнопка «Показать ещё» на фейковом курсоре
  - [x] Переключатель `save` на каждой строке (локальное состояние, без запроса)
  - [x] Индикатор занятого места — на захардкоженном значении квоты
  - [x] Пустое состояние (файлов нет) и состояние загрузки списка (скелетоны)
  - [x] Приёмка владельцем: критерии из спеки руками, обе темы, три языка, 320px
- [x] **022** `api-keys-page-ui`
  - [x] Список ключей (маскированные, `ch_live_a1b2••••••••`) на моке
  - [x] Модалка выпуска нового ключа — полный ключ показывается один раз, копирование в буфер
  - [x] Перевыпуск и отзыв — подтверждающая модалка, состояние меняется локально
  - [x] Приёмка владельцем: критерии из спеки руками, обе темы, три языка, 320px
- [ ] **023** `api-docs-page-ui`
  - [x] Страница `/api-docs`: разделы по эндпоинтам на заглушечной схеме (реальная OpenAPI — в 013)
  - [x] Примеры интеграции (`curl -o`, заголовок `Bearer`) как статичные блоки кода, моноширинный токен `DESIGN.md`
  - [ ] Приёмка владельцем: критерии из спеки руками, обе темы, три языка, 320px

---

# Бэкенд-фаза

Перед стартом — `docker compose up -d` (postgres, redis).

## Стадия 4 — проверка движков

- [ ] **000** `engine-quality-check` — `specs/000-engine-quality-check.md`
  - [x] sharp: JPG↔PNG на 31 реальном файле, зафиксировано качество и время (31/31, avg 48мс)
  - [x] `soffice --headless --convert-to pdf` на 28 реальных docx, зафиксировано (28/28); пароль/кастомные шрифты/макросы — не покрыты, отмечено как гэп, не как «проверено»
  - [x] PDF→DOCX на 36 реальных/сгенерированных файлах, сравнены три кандидата по факту извлекаемого содержимого, не по коду выхода — `TECH-SPEC.md` §2.2
  - [x] Решение по движку PDF→DOCX зафиксировано в `TECH-SPEC.md` §2.2: `pdf2docx`/`PyMuPDF`
  - [ ] Приёмка владельцем: критерии из спеки руками, враждебное второе мнение

## Стадия 5 — `JPG → PNG`, хранилище, глобальный фильтр ошибок

- [ ] **002** 🔒 `convert-jpg-png` — `specs/002-convert-jpg-png.md`
  - [x] Приём файла потоком во временный каталог — под контракт, заданный в 001
  - [x] Определение типа по magic bytes, не по расширению/`Content-Type`
  - [x] Decompression bomb: проверка заявленных размеров до декодирования, лимит 50 Мп
  - [x] `ConversionEngine.convert()` для sharp: `JPG↔PNG`, `quality`, `background`
  - [x] Удаление временных файлов в `finally`
  - [ ] Приёмка владельцем: критерии из спеки руками, враждебное второе мнение
- [ ] **003** 🔒 `file-storage` — `specs/003-file-storage.md`
  - [x] Интерфейс `Storage` (`put`/`getSignedUrl`/`delete`/`list`)
  - [x] `LocalDiskStorage implements Storage`, папка вне репозитория
  - [x] Таблицы `files` и `conversions`, ULID
  - [x] Транзакция: insert `files` + update `users.storage_used_bytes`
  - [x] `GET /v1/files/{id}/download` → `302` на подписанную ссылку (TTL 15 мин)
  - [ ] Приёмка владельцем: критерии из спеки руками, враждебное второе мнение
- [ ] **026** `error-contract-backend` — `specs/026-error-contract-backend.md`
  - [x] `AllExceptionsFilter` → `problem+json` + `request_id`, коды из `packages/shared` (004)
  - [x] Подключить `errorInterceptor` (004) к реальным ответам API
  - [x] Приёмка: зона загрузки (001/006) показывает настоящие коды ошибок, не моковые
  - [ ] Приёмка владельцем: критерии из спеки руками, враждебное второе мнение

## Стадия 6 — остальные направления, подключение зоны загрузки

- [ ] **005** `conversion-engines` (`PNG→JPG`, `PDF→DOCX`) — `specs/005-conversion-engines.md`
  - [x] `PNG→JPG`: `quality` 60–100, `background` — уже работало с 002 (тот же `ImageEngine`), подтверждено здесь curl'ом
  - [x] `PDF→DOCX`: `pdf2docx`/`PyMuPDF` по итогам 000 — дочерний процесс на запрос, системный `python` на `PATH`
  - [x] Лимит одновременных конвертаций на пользователя (3) — ключ `userId ?? hashIp`, в памяти процесса
  - [x] `ConversionEngine` проектируется под HTTP-движок заранее — сигнатура не менялась, новый движок реализует её как есть
  - [x] Подключить 001/006 к реальному `POST /v1/convert`: прогресс через `HttpEventType`, переход в «конвертируем» по 100% загрузки (+ фолбэк на `ResponseHeader`, см. спеку), не по `Response`; реальная отмена — обрыв HTTP
  - [ ] Приёмка владельцем: критерии из спеки руками, враждебное второе мнение

## Стадия 7 — аутентификация

- [ ] **007** 🔒 `auth-native` — `specs/007-auth-native.md`
  - [x] Регистрация email + пароль
  - [x] Вход, единое сообщение «Неверный email или пароль» на любую причину отказа
  - [x] JWT access (15 мин, в памяти) + refresh (30 дней, `HttpOnly`/`Secure` только в production/`SameSite=Lax`)
  - [x] Ротация refresh при обновлении; повторное предъявление использованного токена → завершение всех сессий (кроме окна терпимости ~10с на непосредственно предыдущий — решение владельца, `docs/SECURITY.md`)
  - [x] Клиентская очередь на параллельные `401` (не тройной вызов `/auth/refresh`) — потребовала минимального `GET /v1/me` (решение владельца), иначе нечем проверить
  - [x] Выход
  - [x] Подключить 019 (вход/регистрация/выход) к реальным эндпоинтам
  - [ ] Приёмка владельцем: критерии из спеки руками, враждебное второе мнение
- [ ] **008** 🔒 `auth-providers` — `specs/008-auth-providers.md`
  - [x] Google OAuth 2.0 Authorization Code + PKCE, идентификация по `sub`, не по email
  - [x] Telegram Login Widget — отложено, не в этом чек-листе; кнопка в 019 остаётся мок-заглушкой
  - [x] Таблица `identities` (`provider` + `provider_uid`)
  - [x] Привязка внешнего аккаунта только при подтверждённом email от провайдера
  - [x] Запрет отвязки последнего способа входа
  - [x] Подключить кнопку Google из 019 к реальному потоку (+ профиль 020 — реальный список методов, решение владельца)
  - [ ] Приёмка владельцем: критерии из спеки руками, враждебное второе мнение, полный вход через настоящий Google-аккаунт
- [ ] **009** 🔒 `account-recovery` — `specs/009-account-recovery.md`
  - [x] Канал доставки: MailHog (`docker-compose.yml`) + `nodemailer` (`modules/mail`) — инфраструктура, сделана заранее по просьбе владельца, вне auth-логики (не 🔒 сама по себе)
  - [x] Восстановление пароля: одинаковый ответ независимо от существования аккаунта
  - [x] Токен ≥32 случайных байт, в БД хеш, TTL 30 мин, одноразовый (Telegram-код — отложен вместе с провайдером, `AUTH-RULES.md` §5)
  - [x] Смена пароля из профиля → завершение всех сессий + уведомление
  - [x] Удаление аккаунта — полное каскадное (решение владельца), без повторного ввода пароля (решение владельца)
  - [x] Подключить экраны из 020 к реальным эндпоинтам
  - [ ] Приёмка владельцем: критерии из спеки руками, враждебное второе мнение

## Стадия 8 — хранилище, квота, публичный API

- [ ] **010** 🔒 `user-files-quota` — `specs/010-user-files-quota.md`
  - [x] Курсорная пагинация в БД (`WHERE id < :cursor ORDER BY id DESC`, ULID)
  - [x] Переключатель `save`, автоснятие при заполненной квоте с предупреждением
  - [x] Реальный расчёт `storage_used_bytes`
  - [x] TanStack Query: список файлов + инвалидация `['me']` после мутаций — подключение 021 к реальным данным
  - [ ] Приёмка владельцем: критерии из спеки руками, враждебное второе мнение
- [ ] **011** 🔒 `api-keys` — `specs/011-api-keys.md`
  - [x] Выпуск: `ch_live_` + 32 случайных символа `[a-z0-9]` (~165 бит); `environment` enum `LIVE`/`TEST` в схеме, выпуск пока только `live`
  - [x] В БД — только SHA-256 хеш + префикс для отображения (`ch_live_a1b2`)
  - [x] Полный ключ показывается один раз, при выпуске и перевыпуске (в ответе мутации, не создаётся на клиенте)
  - [x] Перевыпуск инвалидирует предыдущий ключ (`revokedAt` + INSERT новой строки, не UPDATE на месте); `last_used_at` — колонка есть, пишет 012
  - [x] Предел `MAX_ACTIVE_API_KEYS = 3` активных ключей на пользователя (решение владельца) → `API_KEY_LIMIT_REACHED`
  - [x] Подключить 022 к реальным эндпоинтам выпуска/перевыпуска/отзыва (TanStack Query, мок-стор удалён)
  - [ ] Приёмка владельцем: критерии из спеки руками, браузер/Playwright обе темы, враждебное второе мнение
- [ ] **012** 🔒 `public-api` — `specs/012-public-api.md`
  - [x] Bearer по API-ключу (`RequestIdentityService`): `POST /v1/convert`, `GET /v1/files`, download; плохой ключ → `INVALID_API_KEY` (не тихий гость); `last_used_at` fire-and-forget. `PATCH` остаётся под сессией
  - [x] Rate limit token bucket в Redis (`ioredis`, Lua `EVAL`): гость 5/час по хешу IP, пользователь 100/сутки, ключ +60/мин на пользователя; `X-RateLimit-*` + `Retry-After`. Заменил `FixedWindowRateLimiterService` (auth-лимит на том же механизме)
  - [x] `Idempotency-Key` (UUID, 24ч) через Redis: `SET NX` замок, replay без повторной конвертации и без списания лимита, `409` во время выполнения, `422` на не-UUID
  - [x] Redis недоступен → fail-open для rate limit и идемпотентности; проверка ключа/сессии на Postgres/JWT, Redis не на пути (fail-closed по построению)
  - [ ] Приёмка владельцем: критерии из спеки руками, враждебное второе мнение

## Стадия 9 — документация, наблюдаемость, тесты

- [ ] **013** `api-docs` — `specs/013-api-docs.md`
  - [x] OpenAPI 3.1 из Zod-схем `packages/shared` (`@asteasolutions/zod-to-openapi@7` — не 9, та требует zod@4), `GET /v1/openapi.json` (публичный). Только три реально смапленных маршрута публичного API (не восемь из TECH-SPEC §7.2 — половины нет в коде)
  - [x] Подключить 023 к реальной схеме: страница грузит `/v1/openapi.json` (TanStack Query), рисует эндпоинты из неё; стаб `model/endpoint.ts` и `apiDocs.endpoints.*` удалены; плашка при сбое загрузки
  - [ ] Приёмка владельцем: валидатор OpenAPI руками, браузер (обе темы, три языка, 320px), враждебное второе мнение
- [ ] **014** `observability` — `specs/014-observability.md`
  - [x] `GET /health` (мгновенно) и `GET /ready` (БД вниз → `503 down`; Redis вниз → `200 degraded`; всё вверх → `200 ok`), таймаут 1с
  - [x] `request_id` генерируется на клиенте (`req_<uuid>`), интерцептор шлёт `X-Request-Id`; `nestjs-pino` (`genReqId`) кладёт в лог и эхом в заголовок ответа; `request_id` тела ошибки = логовому. Структурные JSON-логи (в проде — сырьё, в деве `pino-pretty`), без заголовков/IP/тел
  - [x] Метрики (`@prometheus-io/client`, `GET /metrics` за `METRICS_TOKEN`): `converthub_conversions_total{direction,status}`, `_conversion_duration_seconds`, `_http_errors_total{code}`, `_conversions_in_flight`, `_storage_used_bytes` + дефолтные `process_*`/`nodejs_*`
  - [ ] Приёмка владельцем: критерии руками (частью прогнаны curl'ом), браузер (`X-Request-Id` в Network), враждебное второе мнение
- [x] **015** `testing` — `specs/015-testing.md`
  - [x] Раннер (Vitest) настроен во всех трёх пакетах + e2e-слой `apps/api` против `convert_hub_test` — сделано
    вне очереди (решение владельца, прямо в `backend`, без ветки/PR)
  - [x] Vitest: 🔒-валидаторы (`magic-bytes`, `conversion-direction`, `pdf-page-count`, `pixel-count`), `ZodValidationPipe` + `convertFormSchema`, автомат зоны загрузки (`Dropzone`), расчёт квоты (`files.service` + e2e `quota.e2e-spec.ts`), `formatBytes`/`formatDate`
  - [x] ~~Testcontainers~~ — заменено второй БД на существующем контейнере Postgres (решение владельца, `specs/015-testing.md`): транзакции и миграции на настоящем PostgreSQL проверяются e2e-слоем `apps/api`
  - [x] Playwright (`apps/web/e2e/`, chromium): гость конвертирует (проверка PNG-сигнатуры скачанного файла); пользователь входит и видит файл; файл >10 МБ отклонён без запроса; конвертация при заполненной квоте не сохраняет файл
  - [x] CI: job `e2e` в `.github/workflows/ci.yml` (postgres+redis, обе БД, `api test:e2e` + Playwright); `ci.yml` заведён вне очереди коммитом `a074ba0`
  - [x] Враждебное второе мнение по тестам (`/code-review`, новый контекст) — 10 находок, устранены
  - [ ] Приёмка владельцем: `pnpm test` / `test:e2e` / `e2e` руками, CI-job зелёный на PR

## Стадия 10 — полный docker compose, `DOCX → PDF`

- [ ] **016** `containerization`
  - [ ] Расширить `docker-compose.yml`: `gotenberg` (без сети наружу, `read_only`, `tmpfs /tmp`, `cap_drop: ALL`, лимит памяти), `minio`
  - [ ] `S3Storage implements Storage`, привязка в DI вместо `LocalDiskStorage` (проверочное свойство: ноль строк в `conversion.service.ts`)
  - [ ] `docker/api.Dockerfile`, multi-stage
  - [ ] `.github/workflows/ci.yml`
- [ ] **018** 🔒 `docx-pdf`
  - [ ] `DocumentEngine implements ConversionEngine` — HTTP POST в Gotenberg
  - [ ] Таймаут `CONVERSION_TIMEOUT`, семафор пула документов (8), ожидание до 10 с → `503` + `Retry-After`
  - [ ] Подключение второй реализации `ConversionEngine` без изменения сигнатуры интерфейса (005) и без изменения `conversion.service.ts`

## Стадия 11 — развёртывание

- [ ] **017** `deployment`
  - [ ] Railway: `api` + `gotenberg` (без публичного домена) + managed Postgres/Redis
  - [ ] Vercel: Angular, Output Directory `dist/web/browser`
  - [ ] `CORS_ORIGIN` — конкретный домен, не `*`
  - [ ] `prisma migrate deploy` при старте контейнера
  - [ ] README для внешнего читателя
